# Mode A static app: no secrets are required.

# Optional local LLM endpoint used only by the browser on the user's own machine.
# Example for Ollama:
# VITE_LOCAL_LLM_ENDPOINT=http://localhost:11434/api/generate
